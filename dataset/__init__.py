"""
Dataset creation packages for DeepResponse.

This package contains dataset builders (e.g. `depmap/`, `ccle/`, `gdsc/`) that
generate processed artifacts under `dataset/<source>/processed/` from raw inputs.

Dataset builders may use curated reference lists stored under `dataset/common/curation/`.
"""
